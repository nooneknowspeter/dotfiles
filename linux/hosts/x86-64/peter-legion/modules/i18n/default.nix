{ config, lib, pkgs, hostname, locale, timezone }:

{

  imports = [ ];

  118n.defaultLocale = locale;

  # console = {
  #   font = "Lat2-Terminus16";
  #   keyMap = "us";
  #   useXkbConfig = true; # use xkb.options in tty.
  # };

}
